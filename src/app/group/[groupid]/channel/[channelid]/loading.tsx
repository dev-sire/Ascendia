import { ChannelSkeleton } from "../../loading"

// Channel switches keep the sidebar/layout intact — only the content area reloads.
// ChannelSkeleton matches the exact 3-col grid of the channel page.
const ChannelLoading = () => <ChannelSkeleton />

export default ChannelLoading